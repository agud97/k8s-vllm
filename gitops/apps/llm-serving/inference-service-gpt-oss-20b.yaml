apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: gpt-oss-20b
  namespace: llm
  annotations:
    serving.kserve.io/deploymentMode: Standard
spec:
  predictor:
    minReplicas: 1
    serviceAccountName: llm-model-sa
    nodeSelector:
      kubernetes.io/hostname: sxmgpu
    tolerations:
      - key: nvidia.com/gpu
        operator: Equal
        value: "true"
        effect: NoSchedule
    model:
      modelFormat:
        name: huggingface
      runtime: vllm-openai-runtime
      storageUri: s3://llm/models/phase-1/openai__gpt-oss-20b
      resources:
        limits:
          nvidia.com/gpu: "1"
          cpu: "8"
          memory: "40Gi"
        requests:
          cpu: "4"
          memory: "20Gi"
      args:
        - --served-model-name=gpt-oss-20b
        - --dtype=bfloat16
        - --max-model-len=2048
        - --gpu-memory-utilization=0.9
        - --trust-remote-code
        - --enforce-eager
